{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package (legacy)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/api",
            "backend": "default"
        }
    ]
}
